services:
  app:
    image: ghcr.io/astral-sh/uv:python3.12-bookworm
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/app
    working_dir: /app
    command: uv run main.py